import 'package:flutter/material.dart';

class LihatLainnya extends StatelessWidget {
  const LihatLainnya({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Lihat Lainnya',
      style: TextStyle(
        color: Colors.blue,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}
