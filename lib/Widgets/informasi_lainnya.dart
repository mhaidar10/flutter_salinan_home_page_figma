import 'package:flutter/material.dart';

class InformasiLainnya extends StatelessWidget {
  const InformasiLainnya({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Informasi Lainnya',
      style: TextStyle(
        color: Colors.blue,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}
