import 'package:flutter/material.dart';

class Destinasi extends StatelessWidget {
  const Destinasi({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Index 2: ini adalah index 2',
      ),
    );
  }
}
